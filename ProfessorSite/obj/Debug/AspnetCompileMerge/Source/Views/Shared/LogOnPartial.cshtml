@if(Request.IsAuthenticated) {
    <strong>@User.Identity.Name</strong>    
     @Html.ActionLink("Αποσύνδεση ", "LogOff", "Account")  
}
else {
    
    @Html.ActionLink("Συνδεση ", "LogOn", "Account")  
}
